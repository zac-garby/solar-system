language: cpp
dist: zesty
addons:
  apt:
    packages:
      - libsfml-dev
      - cmake
script:
  - gcc --version
  - g++ --version
  - cmake --version
  - cmake . && make
  - cat /home/travis/build/Zac-Garby/solar-system/CMakeFiles/CMakeOutput.log
